// Floyd CRUSH Theme Test - JavaScript
// Welcome to the Dark Side of the Code! 🌙

// Keywords (should be pink/purple)
const keyword = "test";
let variable = "Floyd";
var legacy = "AI";

// Functions (should be blue)
function shineOnYouCrazyDiamond(code) {
    return code;
}

// Strings (should be green)
const greeting = "Welcome to the machine!";
const pinkFloyd = "Another brick in the wall";

// Numbers (should be yellow)
const year = 1973;
const pi = 3.14159;

// Types/Classes (should be teal)
class DarkSide {
    constructor(name) {
        this.name = name;
    }
}

// Comments (should be gray)
/* Multi-line comment
   We don't need no education!
*/

// Async/await (keywords should be pink/purple)
async function theWall() {
    await Promise.resolve();
    return true;
}

// Template literals (string should be green)
const band = `Pink ${variable} Floyd`;

// Export (keyword should be pink)
export { shineOnYouCrazyDiamond };
